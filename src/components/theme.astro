---

---

<div class="ml-3 relative" x-data="{ themeOpen: false }">
  <div>
    <button
      type="button"
      class="dark:purple-700 text-sm text-gray-700 font-medium px-4 py-2 border border-purple-300 rounded-md bg-white inline-flex w-full shadow-sm justify-center dark:text-gray-100 focus:outline-none dark:bg-gray-800 hover:bg-purple-100 focus:ring-2 focus:ring-purple-500 focus:ring-offset-2 focus:ring-offset-purple-300 dark:focus:ring-offset-purple-700"
      id="menu-button"
      aria-expanded="true"
      aria-haspopup="true"
      x-on:click="themeOpen = !themeOpen"
    >
      Light/Dark
      <div class="i-heroicons-chevron-down-20-solid ml-2 h-5 w-5 -mr-1" aria-hidden="true"></div>
    </button>
  </div>
  <ul
    x-show="themeOpen"
    x-cloak
    x-transition:enter="transition ease-out duration-100"
    x-transition:enter-start="transform opacity-0 scale-95"
    x-transition:enter-end="transform opacity-100 scale-100"
    x-transition:leave="transition ease-in duration-75"
    x-transition:leave-start="transform opacity-100 scale-100"
    x-transition:leave-end="transform opacity-0 scale-95"
    class="text-base mt-1 py-1 rounded-md bg-white max-h-56 w-full ring-1 ring-black ring-opacity-5 shadow-lg absolute z-20 overflow-auto sm:text-sm focus:outline-none dark:bg-gray-700"
    tabindex="-1"
    role="listbox"
    aria-labelledby="listbox-label"
    aria-activedescendant="listbox-option-3"
  >
    <li
      class="text-gray-900 py-2 pl-2 pr-4 cursor-default select-none relative dark:text-white hover:text-white dark:bg-gray-700 hover:bg-purple-600 dark:hover:text-gray-900 dark:hover:bg-purple-300"
      id="listbox-option-0"
      x-on:click="localStorage.removeItem('theme'); window.location.reload()"
    >
      <div class="flex items-center">
        <div class="i-heroicons-star h-6 w-6"></div>
        <!-- Selected: "font-semibold", Not Selected: "font-normal" -->
        <span
          class="ml-1 block truncate"
          x-bind:class="localStorage.theme ? 'font-normal' : 'font-semibold'">System</span
        >
      </div>
      <span
        x-show="!localStorage.theme"
        class="text-purple-600 pr-4 flex items-center inset-y-0 right-0 absolute hover:text-white"
      >
        <!-- Heroicon name: solid/check -->
        <div class="i-heroicons-check-20-solid h-5 w-5"></div>
      </span>
    </li>
    <li
      class="text-gray-900 py-2 pl-2 pr-4 cursor-default select-none relative dark:text-white hover:text-white dark:bg-gray-700 hover:bg-purple-600 dark:hover:text-gray-900 dark:hover:bg-purple-300"
      id="listbox-option-0"
      x-on:click="localStorage.theme = 'light'; window.location.reload()"
    >
      <div class="flex items-center">
        <div class="i-heroicons-sun h-6 w-6"></div>
        <!-- Selected: "font-semibold", Not Selected: "font-normal" -->
        <span
          class="ml-1 block truncate"
          x-bind:class="localStorage.theme == 'light' ? 'font-semibold' : 'font-normal'">Light</span
        >
      </div>
      <span
        x-show="localStorage.theme == 'light'"
        class="text-purple-600 pr-4 flex items-center inset-y-0 right-0 absolute hover:text-white"
      >
        <div class="i-heroicons-check-20-solid h-5 w-5"></div>
      </span>
    </li>
    <li
      class="text-gray-900 py-2 pl-2 pr-4 cursor-default select-none relative dark:text-white hover:text-white dark:bg-gray-700 hover:bg-purple-600 dark:hover:text-gray-900 dark:hover:bg-purple-300"
      id="listbox-option-0"
      x-on:click="localStorage.theme = 'dark'; window.location.reload()"
    >
      <div class="flex items-center">
        <div class="i-heroicons-moon-solid h-6 w-6"></div>
        <!-- Selected: "font-semibold", Not Selected: "font-normal" -->
        <span
          class="ml-1 block truncate"
          x-bind:class="localStorage.theme == 'dark' ? 'font-semibold' : 'font-normal'">Dark</span
        >
      </div>
      <span
        x-show="localStorage.theme == 'dark'"
        class="text-purple-600 pr-4 flex items-center inset-y-0 right-0 absolute hover:text-white"
      >
        <div class="i-heroicons-check-20-solid h-5 w-5"></div>
      </span>
    </li>
  </ul>
</div>
